const { defaultAccounts } = require("@ethereum-waffle/provider");
const { ethers } = require("hardhat");

// scripts/index.js
async function main() {
  // contract test scripting goes here
    
    
  }
  
  main()
    .then(() => process.exit(0))
    .catch(error => {
      console.error(error);
      process.exit(1);
    });